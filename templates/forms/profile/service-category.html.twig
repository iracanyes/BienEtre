{{
    form_start(
        form,
        {
            "method":"POST",
            "action":path('service_category_add'),
            "attr":{"class":"form-horizontal"}
        }
    )
}}

{# Erreurs du formulaire #}
{{ form_errors(form) }}


<div class="form-group">

    <label for="{{ form.name.vars.id }}"
           class="col-sm-2 control-label">
        {{ form.name.vars.label }}
    </label>
    {{ form_errors(form.name) }}
    <div class="col-sm-10">
        {{ form_widget(form.name, {"attr":{"class":"form-control"}}) }}
    </div>

</div>

<div class="form-group">
    <label for="{{ form.description.vars.id }}"
           class="col-sm-2 control-label">
        {{ form.description.vars.label }}
    </label>
    {{ form_errors(form.description) }}
    <div class="col-sm-10">
        {{ form_widget(form.description, {"attr":{"class":"form-control"}}) }}
    </div>

</div>


<div class="checkbox">

    {{ form_errors(form.inFrontPage) }}
    <div class="col-sm-2">

    </div>
    <div class="col-sm-10">
        {{ form_widget(form.inFrontPage, {"attr":{"class":""}}) }}
        <label for="{{ form.inFrontPage.vars.id }}"
               class="">
            {{ form.inFrontPage.vars.label }}
        </label>
    </div>





</div>

<div class="form-group">

    {{ form_errors(form.image) }}
    <label for="{{ form.image.place.vars.id }}"
           class="col-sm-2 control-label">
        {{ form.image.place.vars.label }}
    </label>
    <div class="col-sm-10">
        {{ form_widget(form.image.place, {"attr":{"class":"form-control"}}) }}

    </div>


</div>

<div class="form-group">

    {{ form_errors(form.image) }}
    <label for="{{ form.image.url.vars.id }}"
           class="col-sm-2 control-label">
        {{ form.image.url.vars.label }}
    </label>
    <div class="col-sm-10">
        {{ form_widget(form.image.url, {"attr":{"class":""}}) }}

    </div>


</div>


<input type="hidden" name="_csrf_token" value="{{ csrf_token("authenticate") }}">

{{ form_widget(form.submit) }}

{# Reste du formulaire#}
{#
{{ form_rest(form) }}
#}



{# End Form #}
{{ form_end(form) }}
