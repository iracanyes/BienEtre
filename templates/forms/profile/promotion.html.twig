{{
form_start(
    form,
    {
        "method":"POST",
        "action": path,
        "attr":{"class":"form-horizontal"}
    }
)
}}

{# Erreurs du formulaire #}
{{ form_errors(form) }}


<div class="form-group">
    <label for="{{ form.name.vars.id }}"
           class="col-sm-2 control-label">
        {{ form.name.vars.label }}
    </label>
    {{ form_errors(form.name) }}
    <div class="col-sm-10">
        {{ form_widget(form.name, {"attr":{"class":"form-control"}}) }}
    </div>
</div>

<div class="form-group">
    <label for="{{ form.description.vars.id }}"
           class="col-sm-2 control-label">
        {{ form.description.vars.label }}
    </label>
    {{ form_errors(form.description) }}
    <div class="col-sm-10">
        {{ form_widget(form.description, {"attr":{"class":"form-control"}}) }}
    </div>

</div>

<div class="form-group">
    <label for="{{ form.pdf.vars.id }}"
           class="col-sm-2 control-label">
        {{ form.pdf.vars.label }}
    </label>
    {{ form_errors(form.pdf) }}

    <div class="col-sm-10">
        {{ form_widget(form.pdf, {"attr":{"class":""}}) }}
    </div>

</div>

<div >
    <div class="form-group">
        <label for="{{ form.startDate.vars.id }}"
               class="col-sm-2 control-label">
            {{ form.startDate.vars.label }}
        </label>
        {{ form_errors(form.startDate) }}
        <div class="col-sm-10">
            {{ form_widget(form.startDate, {"attr":{"class":"col-sm-2 control-label"}}) }}
        </div>

    </div>

    <div class="form-group">
        <label for="{{ form.endDate.vars.id }}"
               class="col-sm-2 control-label">
            {{ form.endDate.vars.label }}
        </label>
        {{ form_errors(form.endDate) }}
        <div class="col-sm-10">
            {{ form_widget(form.endDate, {"attr":{"class":"col-sm-2 control-label"}}) }}
        </div>

    </div>

    <div class="form-group">
        <label for="{{ form.releaseDate.vars.id }}"
               class="col-sm-2 control-label">
            {{ form.releaseDate.vars.label }}
        </label>
        {{ form_errors(form.releaseDate) }}
        <div class="col-sm-10">
            {{ form_widget(form.releaseDate, {"attr":{"class":"col-sm-2 control-label"}}) }}
        </div>

    </div>

    <div class="form-group">
        <label for="{{ form.expiryDate.vars.id }}"
               class="col-sm-2 control-label">
            {{ form.expiryDate.vars.label }}
        </label>
        {{ form_errors(form.expiryDate) }}
        <div class="col-sm-10">
            {{ form_widget(form.expiryDate, {"attr":{"class":"col-sm-2 control-label"}}) }}
        </div>

    </div>
</div>


<div class="form-group">
    <label for="{{ form.serviceCategory.vars.id }}"
           class="col-sm-2 control-label">
        {{ form.serviceCategory.vars.label }}
    </label>

    {{ form_errors(form.serviceCategory) }}
    <div class="col-sm-10">
        {{ form_widget(form.serviceCategory) }}
    </div>

</div>



<input type="hidden" name="_csrf_token" value="{{ csrf_token("authenticate") }}">

{{ form_widget(form.submit) }}

{# Reste du formulaire #}

{{ form_rest(form) }}




{# End Form #}
{{ form_end(form) }}
